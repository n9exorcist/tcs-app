import { SortableColumnDirective } from './sortable-column.directive';

describe('SortableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
